type Note @model {
  id: ID!
  name: String!
  description: String
}
type AmplifyNetbox3 @model {
  id: ID!
  systemIdentifier: String!
  clientId: String!
  createdAt: AWSDateTime
  updatedAt: AWSDateTime
}
